title: "TF 5 - TPs"
theme: minima

markdown: kramdown

collections:
  tp:
    output: true
    permalink: /:collection/:name/

defaults:
  - scope:
      path: ""
      type: "tp"
    values:
      layout: "tp"